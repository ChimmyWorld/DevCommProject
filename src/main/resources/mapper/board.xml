<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbank.model.BoardDAO">

<select id="selectPreview"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board
	where type = #{idx}
	order by idx desc
	offset 0 rows
	fetch first 5 rows only
</select>

<select id="selectList"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board where type = #{type}
	order by idx desc
</select>

<select id="selectOne"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board where idx = #{idx}	
</select>

<insert id="insert"
	parameterType="com.itbank.model.vo.BoardVO">
	insert into board
	(type, title, contents, writer, article_img) values
	(#{type}, #{title}, #{contents}, #{writer}, #{article_img, jdbcType=VARCHAR})	
</insert>

<delete id="delete"
		parameterType="int">
	delete from board where idx = #{idx}
</delete>

<update id="update"
		parameterType="com.itbank.model.vo.BoardVO">
	update board
	set title=#{title}, contents=#{contents}
	where idx=#{idx}		
</update>

<update id="countUp"
		parameterType="int">
	update board
	set view_count = view_count + 1
	where idx = #{idx}		
</update>

<select id="selectMyBoardList"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board where writer = #{nick}
	order by idx desc
</select>

<select id="selectIdx"
	resultType="string">
	select idx from board
		order by idx desc
		fetch first rows only
</select>

<!-- 추천 -->
<select id="selectRecommend"
	resultType="int">
	select good from recommend where board_idx = #{idx}
	and recommender = #{recommender}
</select>

<select id="selectRecCnt"
	resultType="int">
	select count(*) as count from recommend where board_idx = #{arg0} and good = #{arg1}
</select>

</mapper>
