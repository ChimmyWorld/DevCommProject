<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbank.model.BoardDAO">

<select id="selectPreview"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board
	where type = #{idx}
	order by idx desc
	offset 0 rows
	fetch first 5 rows only
</select>

<select id="selectList"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board where type = #{type}
	order by idx desc
</select>

<select id="selectOne"
	resultType="com.itbank.model.vo.BoardVO">
	select * from board where idx = #{idx}	
</select>

<insert id="insert"
	parameterType="com.itbank.model.vo.BoardVO">
	insert into board
	(type, title, contents, writer) values
	(#{type}, #{title}, #{contents}, 'asdf')	
</insert>

<delete id="delete"
		parameterType="int">
	delete from board where idx = #{idx}
</delete>

<update id="update"
		parameterType="com.itbank.model.vo.BoardVO">
	update board
	set title=#{title}, contents=#{contents}
	where idx=#{idx}		
</update>
	
</mapper>
